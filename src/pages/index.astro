---
import entries from "../json/2021.json";
const title = "learn--astro";
---
<html>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width">
<title>{title}</title>
<style>
</style>
<body>

<h1>{title}</h1>

<h2>entries</h2>

<ul>
{ entries.map(({date, title}) => {
	<li><a href={`/entry/${[date, title].join("-").replaceAll(" ", "-")}`}>{title}</a> ({date})</li>
})}
</ul>

</body>
</html>
